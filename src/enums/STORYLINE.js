import {ENEMY} from "./ENEMY.js";

const basicStory = {
    enemies: [
        ENEMY.slime,
        ENEMY.skeleton,
        ENEMY.babyBoss,
        ENEMY.pickachu,
        ENEMY.evilNatas,
        ENEMY.eyyabThePredator,
        ENEMY.mikaTheExiled,
    ],
}

// Dit is bedoelt voor meerdere endings maar ik had niet genoeg tijd.
export const STORYLINE = {
    basicStory: basicStory,
}